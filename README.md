# Register

## Build Setup

```bash

$ yarn install

# install dependencies
$ cd registers
$ yarn

# serve with hot reload at localhost:3000
$ yarn dev


$ cd api
$ yarn

# serve with hot reload at localhost:2000
$ yarn dev

